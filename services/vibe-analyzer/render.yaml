services:
  - type: web
    name: rippl-vibe-analyzer
    runtime: python
    plan: free
    rootDir: services/vibe-analyzer
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
